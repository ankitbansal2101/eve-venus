{
  "name": "venus-inventory-system",
  "version": "1.0.0",
  "description": "Web-based inventory system for mid-sized manufacturers",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": ["inventory", "manufacturing", "dashboard", "react", "node"],
  "author": "VENUS Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
